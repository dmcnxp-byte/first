# page-builder

SectionRenderer + one adapter component per registered Sanity page-builder
block. See ../../DOC/PAGE_BUILDER_ARCHITECTURE.md.

Populated in Phase 3, alongside the corresponding Sanity block schemas.
