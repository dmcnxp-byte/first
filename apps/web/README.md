# apps/web

The Next.js application (App Router). Run everything from the repo root — see
the top-level [README.md](../../README.md) for setup and the `/DOC` folder for
the approved architecture this app implements.
